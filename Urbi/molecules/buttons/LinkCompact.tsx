import * as React from "react";
import Link, { LinkProps } from "Urbi/molecules/buttons/Link";

export const LinkCompact = (props: LinkProps) => <Link {...props} compact />;

export default LinkCompact;
